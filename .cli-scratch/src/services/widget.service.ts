// src/services/widget.service.ts
// Widget service — auto-generated by street generate.

import { Injectable } from 'streetjs';
import { WidgetRepository } from '../repositories/widget.repository.js';

export interface Widget {
  id: string;
  createdAt: Date;
  updatedAt: Date;
  [key: string]: unknown;
}

@Injectable()
export class WidgetService {
  constructor(private readonly repository: WidgetRepository) {}

  async findAll(page: number, limit: number) {
    return this.repository.findAll(page, limit);
  }

  async findById(id: string): Promise<Widget | null> {
    return this.repository.findById(id);
  }

  async create(input: Record<string, unknown>): Promise<Widget> {
    const now = new Date();
    const item: Widget = {
      id: crypto.randomUUID(),
      ...input,
      createdAt: now,
      updatedAt: now,
    };
    await this.repository.create(item);
    return item;
  }

  async update(id: string, input: Record<string, unknown>): Promise<Widget | null> {
    const existing = await this.repository.findById(id);
    if (!existing) return null;

    const updated: Widget = {
      ...existing,
      ...input,
      updatedAt: new Date(),
    };
    await this.repository.update(updated);
    return updated;
  }

  async delete(id: string): Promise<void> {
    await this.repository.delete(id);
  }
}
