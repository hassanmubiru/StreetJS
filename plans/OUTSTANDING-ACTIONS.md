# StreetJS — Outstanding Actions (Master Register)

> Single consolidated to-do aggregating every open item across all audit, security,
> and governance docs. Owner tags:
> **[OPERATOR]** platform/destructive (GitHub settings, history, secrets) ·
> **[MAINTAINER]** repo edit (docs/config, non-runtime) ·
> **[RUNTIME]** plugin/core code change (out of governance scope — separate PR).
>
> Everything below is what remains; all governance/organization/CI/docs hardening
> already applied is recorded in `audits/ENTERPRISE-AUDIT.md` (canonical) and the
> `CHANGELOG.md` `[Unreleased]` section.

## ✅ Completed since this register was created (verified)
- **#10 app-\* → `examples/scaffold-*`** — moved; `zizmor.yml` + `dependabot.yml` updated; no functional refs left.
- **#11 SEO files** — `BingSiteAuth.xml` / `googledf*.html` **removed** from the repo + dropped from the root allowlist. *(Operator: re-add them in the website repo.)*
- **#16 Compose ports** — all 11 host ports bound to `127.0.0.1`; `docker compose config` validates all 6.
- **#23 `prereqs.mjs` cloudflare path** — fixed to `infra/examples/cloudflare/wrangler.toml`.
- **Operator tooling generated:** `.github/repository-settings.json`, `security/KEY-ROTATION-CHECKLIST.md`, `security/KEY-EMERGENCY-RUNBOOK.md`, `security/SECRET-SCANNING-GUIDE.md`.
- **Supply chain:** `scripts/security/verify-release.mjs` + `npm run verify:signatures` (21/21 verified).
- **Report consolidation:** `audits/REPORT-INDEX.md` (canonical map); `PHASE-20-COMPLETION-REPORT.md` archived.
- **#6 (partial) Support matrix** — `docs/enterprise/support-matrix.md` created (version support + backport + platform/runtime matrix). *Remaining: commercial-support SLA tiers (operator, only if a commercial offering exists).*
- **Foundation readiness** — `governance/DECISION-PROCESS.md` created (roles, lazy consensus, RFC workflow, voting/escalation, neutrality). *Remaining gating items: multi-org neutral maintainership + trademark policy (operator/community).*
- **#22 CI `retention-days` + `concurrency` standardized** — all 38 workflows now declare a top-level `concurrency` group (`<workflow>-${{ github.ref }}`; `cancel-in-progress: true` for PR/push verification + security gates, `false` for release/deploy/admin/soak); all 21 `upload-artifact` steps now set `retention-days` (coverage 14, evidence/verification 30, release/SBOM/signed/cert 90, SARIF 5). Validated: 38/38 YAML parse clean.
- **Operator P0 runbook** — `security/OPERATOR-EXECUTION-CHECKLIST.md` created: sequenced, copy-paste `gh` commands for branch/push protection, secret-scanning, history purge, key relocation, signed commits, PGP key (references the per-topic reviews, no duplication).

## P0 — Critical
| # | Action | Owner | Source | Done when |
|---|---|---|---|---|
| 1 | ✅ **Done (operator-verified 2026-06-27)** — branch protection on `main`: 11 required checks, `require_code_owner_reviews: true`, `required_approving_review_count: 1`, `required_linear_history: true`, `allow_force_pushes: false`, `enforce_admins: false` (set false so the **solo** admin can merge own PRs — GitHub forbids self-approval; re-enable once a 2nd maintainer exists, item #28). *(Live set = 11 verified-present checks; security-workflow contexts in `repository-settings.json` to be added after they report on a PR.)* | [OPERATOR] | `security/BRANCH-PROTECTION-REVIEW.md`, `OPENSSF-REVIEW.md`, `SLSA-ASSESSMENT.md` | Settings applied + a blocked direct push confirms |
| 2 | ✅ **Done (operator-verified 2026-06-27)** — Secret Scanning + Push Protection + Dependabot security updates enabled. Evidence: `gh api repos/hassanmubiru/StreetJS --jq '.security_and_analysis'` → `secret_scanning`, `secret_scanning_push_protection`, `dependabot_security_updates` all `enabled`. *(Optional, still off: `secret_scanning_non_provider_patterns`, `secret_scanning_validity_checks`.)* | [OPERATOR] | `OPENSSF-REVIEW.md`, `TRUST-CENTER.md` | Toggles on in repo security settings |
| 3 | ◑ **Substantially done (operator-verified 2026-06-27)** — history rewritten (`git filter-repo`); `main`+tags+branches force-pushed to cleaned history. **Verified:** normal clone `git log --all -- street-signing.key.pem` is **empty**; `for-each-ref --contains d7bbfc40` returns only `refs/pull/*`. **Only residual:** PR refs + GitHub cache → **GitHub Support request** required to fully purge (reference `d7bbfc40…`). Keep `.gitleaks.toml` allowlist until Support confirms. | [OPERATOR] | `security/KEY-ROTATION-RUNBOOK.md` §7 | Support purges PR-refs/cache; allowlist removed |
| 4 | ✅ **Done (operator-verified 2026-06-28)** — no private key material in the working tree: `find` for `*.key.pem`/`*.pkcs8.pem`/`*.key`/`*.p12`/`*.pfx` is empty. The live signing key was temporarily restored to `keys/street-signing-2026.key.pem` during the 2026-06-28 plugin re-publish (item #29), then `shred -u`'d once set into the `STREET_PLUGIN_SIGNING_KEY` CI secret; `keys/` verified empty afterward. Sole accessible copies now: the write-only CI secret + the operator's secrets manager. | [OPERATOR] | `KEY-ROTATION-RUNBOOK.md` §8, `SECURITY-CLASSIFICATION.md` | No key files in the working tree |
| 5 | ✅ **Done (operator-verified 2026-06-27)** — `required_signatures.enabled: true` on `main` (per branch-protection API). Future commits to `main` must be signed. | [OPERATOR] | `CONTRIBUTOR-GOVERNANCE.md`, threat model | Branch rule requires signatures |
| 29 | ✅ **Done (operator-verified 2026-06-28)** — re-signed + re-published all official `@streetjs/plugin-*` to npm under the rotated anchor `3ae9add0…`. **Root cause:** registry packages (`1.0.2`) were signed by the pre-rotation key, and the `STREET_PLUGIN_SIGNING_KEY` CI secret had been stored in a non-PKCS#8 form (Node `createPrivateKey` → `ERR_OSSL_UNSUPPORTED`), later clobbered to empty by failed-path attempts. **Fix:** bumped all plugins to `1.0.3`, re-set the secret from the validated key (`ed25519`, pub-sha256 `3ae9add0…`) via `gh secret set < keys/street-signing-2026.key.pem` from the repo root, re-ran `publish-plugins.yml` (green). **Verified:** `scripts/verify-official-signatures.mjs` → 18/18 ✓; `Runtime Certification` (`certify`/`verify:runtime`) green on `main`. Also bumped `upload-artifact`→v7.0.1 (Node 24) in `runtime-certification.yml`+`soak-scale-chaos.yml` (PR #96) to clear the Node 20 deprecation. | [OPERATOR] | `verify-official-signatures.mjs`, `publish-plugins.yml` | `verify-official-signatures` exits 0; `certify` green |

## P1 — High
| # | Action | Owner | Source | Done when |
|---|---|---|---|---|
| 6 | ◑ **Functional goal met; team upgrade blocked on org move** — live `.github/CODEOWNERS` resolves to `@hassanmubiru` (valid owner), so "require Code-Owner review" **already works**. Team-based ownership is **impossible today**: verified `orgs/streetjs` → 404 (no such org) and repo owner `hassanmubiru` is a **User** account (personal accounts can't have teams). **Prerequisite (operator/org):** create a `streetjs` org → transfer the repo → create teams; *then* map `CODEOWNERS.proposed` placeholders to real `@streetjs/<slug>` names. Do NOT fill `@org/*` while personally owned (would void the Code-Review gate). | [OPERATOR] | `CONTRIBUTOR-GOVERNANCE.md` | Repo under an org with real teams as code owners |
| 7 | ✅ **Done** — generated `package-lock.json` in all 4 `web/` apps (`npm install --package-lock-only`; vite resolves to 6.4.3, `npm audit` → 0 vulns in each); lockfiles trackable + already referenced by `dependabot.yml`. Stale "run npm install" NOTE removed | [MAINTAINER] | cleanup plan, Dependabot | Lockfiles committed; Dependabot tracks them |
| 8 | ✅ **Done** — outbound HTTP **timeout** (optional `timeoutMs`, default 30s, enforced via `req.setTimeout`+`destroy`) added to all 9 `node:https` plugins: stripe/twilio/sendgrid/auth0 (core) + paypal/openai/clerk/firebase/supabase (packages). Additive/backward-compatible; per-plugin timeout-validation tests added; all builds + suites green; 21/21 manifest signatures still verify | [RUNTIME] | `PLUGIN-SECURITY-REPORT.md`, `PLUGIN-MATURITY-MATRIX.md` | Each client enforces a timeout; tests added |
| 9 | ✅ **Done** — constant-time **webhook verifiers** shipped + tested for all four signing providers: `verifyStripeWebhook` (HMAC-SHA256, replay tolerance), `verifyTwilioSignature` (HMAC-SHA1 URL+params), `verifySendGridWebhook` (ECDSA-P256 event webhook), `verifyPayPalWebhook` (RSA-SHA256 local cert verification). Stripe/Twilio/SendGrid exported from `streetjs`; PayPal from `@streetjs/plugin-paypal` | [RUNTIME] | `PLUGIN-SECURITY-AUDIT.md`, `OWASP-ASVS-MAPPING.md` V13 | Constant-time verifier + tests per plugin |
| 10 | ✅ **Done** — `app-*` scaffolds relocated to `examples/scaffold-*`; `zizmor.yml` + Dependabot dirs updated; no functional refs left (see Completed section) | [MAINTAINER] | `REPOSITORY-CLEANUP-PLAN.md`, `DOCKER-REVIEW.md` | `app-*` gone from root; CI green |
| 11 | Move SEO files (`BingSiteAuth.xml`, `googledf*.html`) to the website repo, then `git rm` here | [OPERATOR] | cleanup plan, `SECURITY-CLASSIFICATION.md` | Files removed; root allowlist updated |

| 30 | **Redis Cluster / PostgreSQL HA support is absent, not just untested.** Verified by direct inspection: `RedisClientOptions` (`packages/core/src/transports/resp.ts`) is `{ host?, port?, password? }` — no `nodes[]`/`sentinels[]` field, no `MOVED`/`ASK` cluster-redirect handling. `PgConnectOptions` (`packages/core/src/database/wire.ts`) is `{ host, port, user, password, database }` — single endpoint only, no replica/standby/failover field. Neither client can participate in a Redis Cluster topology or a PostgreSQL HA/failover setup. Distinct from the NATS gap (item — live NATS integration test added, `packages/plugin-nats/test/broker.it.test.mjs`, 4/4 pass against a real broker): that was a missing **test** against an existing client; this is a missing **capability** in the client itself, so no test can honestly close it without first extending the client. If cluster Redis or HA PostgreSQL are supported deployment targets, the clients need multi-seed-node config + redirect/failover handling before that configuration is production-supported. | [RUNTIME] | direct client-signature inspection (this audit) | `RedisClientOptions`/`PgConnectOptions` accept multi-node/HA config + a live-cluster/HA integration test passes |

| 31 | **"Release Engineering Enforcement" CI job unconditionally fails on `workflow_dispatch`/`release` — verified root cause.** `scripts/release/render-report.mjs` correctly implements zero-trust evidence (no `release-inputs.json` ⇒ scores default to 0 ⇒ gate fails, by design — Req 11.6, `.github/workflows/ci-cd-enforcement.yml` "Build release report" step). But `release-inputs.json` is **gitignored** (`.gitignore` line 109, commented "Generated build/release artifact — produced in CI, attached to releases") and **no step in any workflow actually generates it**. A maintainer-authored copy exists locally with real, dated, cited evidence (OpenSSF Scorecard 7.3, measured 77.03% line coverage from a specific CI run, etc.) but is never committed or supplied to the runner, so the job's `if [ -f release-inputs.json ]` is always false on a clean checkout — reproduced via `gh workflow run ci-cd-enforcement.yml` (run 28755115205): job fails every time, not intermittently. **Do not "fix" by committing the static file or fabricating scores** — that would defeat the zero-trust design this file protects (Req 11.6 is intentional). Real fix is a CI step that *derives* `release-inputs.json` from live sources already referenced in the maintainer's `_evidence` block (OpenSSF Scorecard API, the core-coverage `c8`/lcov artifact, `npm audit`), or an explicit, secret-backed maintainer-supplied input for the rubric-based dimensions (reliability/performance) that aren't machine-derivable. | [MAINTAINER] | `scripts/release/render-report.mjs`, `.github/workflows/ci-cd-enforcement.yml` (this audit) | A `release-inputs.json`-generation (or maintainer-supplied) step exists in CI; `Release Engineering Enforcement` passes on a real `workflow_dispatch`/`release` run without a manually pre-placed file |

| 32 | ✅ **Fixed (this audit)** — `provider-integration.yml`'s `storage-providers` job was **false-green**: it ran `npm run test -w packages/storage` (`node --test dist/tests/*.test.js`) without ever building `packages/storage` first (unlike the sibling `search-providers` job, whose `test` script self-builds via `tsc &&`). On **Node ≥22**, `node --test` against a glob matching zero files silently reports `# tests 0 / # pass 0 / # fail 0` and **exits 0** (Node 20 correctly errors "Could not find"), so the job had been passing while running **zero** of its real Postgres/GCS/Azurite provider tests since introduction. Verified via `gh api .../actions/jobs/{id}/logs` on run 28755739760 (pre-fix, 0 tests) vs 28755961571 (post-fix, 370 tests/363 pass/0 fail/7 skip against live service containers). **Fix:** added an explicit `npm run build -w packages/storage` step before the test step, matching the pattern used by every pillar's dedicated integration workflow (e.g. `realtime-integration.yml`). **Secondary, smaller gap noted but not fixed:** the `[integration] gcs/azure driver live round-trip` tests still skip in this job — they gate on `GCS_BUCKET`/`AZURE_STORAGE_CONNECTION_STRING`-style env vars that aren't wired to the job's already-running `fake-gcs`/`azurite` emulators, even though the emulator-backed unit tests (byte round-trip, existence, metadata) now genuinely execute. | [RUNTIME] | `.github/workflows/provider-integration.yml`, `packages/storage/package.json` (this audit) | `storage-providers` job runs a non-zero, real test count every dispatch (done); optionally wire the live-round-trip tests to the emulator ports so they run instead of skip |

## P2 — Medium
| # | Action | Owner | Source |
|---|---|---|---|
| 12 | Migrate plugin signing to **keyless (Sigstore/OIDC)** or KMS/HSM (reaches SLSA L3, removes long-lived key) | [OPERATOR]+[MAINTAINER] | `SLSA-ASSESSMENT.md`, `SECURITY-ROADMAP.md` |
| 13 | ✅ **Done** — `.github/workflows/verify-signatures.yml` runs `npm run verify:signatures` as a **fatal** gate (every plugin `manifest.signed.json` signature must verify against the embedded anchor, not just the `manifest.pub` fingerprint that `verify-signing-anchor` already checks) + `npm audit signatures` (informational dependency-tree provenance). Path-filtered to signing-relevant changes; intentionally **not** a required status check (a path-filtered required check would deadlock unrelated PRs in "Expected" — the always-on `verify-signing-anchor` stays the required gate). Verified locally: 21/21 ✓, exit 0 | [MAINTAINER] | `SLSA-ASSESSMENT.md`, `NPM-PUBLISH-SECURITY-REVIEW.md` |
| 14 | ✅ **Done** — Generated `verification-artifacts/` no longer tracked (4 stale files `git rm --cached`; dir already gitignored + CI-regenerated by `deploy-verify`/`upgrade-codemods`); SBOM stays a CI-uploaded release asset | [MAINTAINER] | `SECURITY-CLASSIFICATION.md`, `REPOSITORY-ORGANIZATION.md` |
| 15 | ✅ **Done** — additive **TLS surface** (`tls`/`tlsRejectUnauthorized`/`tlsServerName`/`tlsCa`, default plain TCP) shipped for **all 5**: redis + mongodb + kafka (SSL/SASL_SSL) + rabbitmq (AMQPS) use connect-from-start `tls.connect()`; **nats** performs the protocol **STARTTLS** upgrade after the plaintext `INFO`. Config-validation unit tests added for each (redis/mongodb/kafka/rabbitmq/nats); plaintext paths unchanged. TLS handshakes exercised in integration when a TLS endpoint is present | [RUNTIME] | `OWASP-ASVS-MAPPING.md` V9, `PLUGIN-SECURITY-AUDIT.md` |
| 16 | Bind local compose DB ports to `127.0.0.1` | [MAINTAINER] | `INFRASTRUCTURE-SECURITY-REVIEW.md` |
| 17 | ◑ **Partial** — docs are **already searchable** (just-the-docs Lunr, `search_enabled`); enhanced the `search:` config (h3 indexing, content previews, `s` focus shortcut). Added a documented **versioning** policy + surface: `docs/versions.md` (+ "Versions" aux link) tying current `site.version` → support-matrix/changelog/tagged source; gave `docs/enterprise/support-matrix.md` the front matter it was missing so it now renders + is searchable. *Remaining: browsable multi-version doc trees (roadmap) — Algolia/Pagefind unnecessary at this size* | [MAINTAINER] | `ENTERPRISE-READINESS-2026.md` |
| 18 | ◑ **Partial** — fuzz suite + property-based tests present (`ci-cd.yml` `system-tests` `fuzz-testing`) and documented in `OPENSSF-REVIEW.md`. *Remaining: OSS-Fuzz onboarding = submit a project config to the external OSS-Fuzz repo (operator/maintainer, not repo-completable)* | [MAINTAINER]+[OPERATOR] | `OPENSSF-REVIEW.md`, `SECURITY-ROADMAP.md` |
| 19 | ✅ **Done** — `docs/security/dashboard.md` surfaces live badges (CI, CodeQL, OpenSSF Scorecard, provenance) + a control-posture table linking each gate to its workflow, the live GitHub Security tabs, and canonical docs; live/operator-setting values are marked **UNVERIFIED** rather than fabricated | [MAINTAINER] | `SECURITY-ROADMAP.md` |
| 20 | Add a real **PGP key** to `SECURITY.md` (replace placeholder; never commit the private key) | [OPERATOR] | `SECURITY.md` |
| 21 | ◑ **In progress** — `PLUGIN-MATURITY-MATRIX.md` refreshed (timeouts ✅ on 9 HTTP plugins; **all 4** webhook verifiers ✅; TLS ✅ on all 5); `docs/plugins/webhook-verification.md` guide added. **Verified:** runnable `example/index.mjs` already ships for 20/21 plugin packages (marzpay uses full react/next apps); all 20 pass `node --check`. **CI baseline added:** `npm run test:plugins-offline` (`scripts/test-plugins-offline.mjs`) builds core + each plugin, runs node:test (15/15 passed locally), and syntax-checks all 20 examples; wired via `.github/workflows/plugin-tests.yml`. *Remaining: standalone example **apps** per plugin + raised coverage gates to flip ◑→✅* | [RUNTIME]+[MAINTAINER] | `PLUGIN-MATURITY-MATRIX.md` |
| 22 | ✅ **Done** — Standardize CI `retention-days` + `concurrency` across workflows (see Completed section) | [MAINTAINER] | `STREETJS-FULL-AUDIT-REPORT.md` Phase 12 |
| 23 | Fix latent bug: `scripts/cloud/prereqs.mjs` cloudflare path (`deploy/cloudflare-workers` → `infra/examples/cloudflare`) | [MAINTAINER] | `PHASE-19-MASTER-AUDIT.md` B-1 |
| 33 | ⛔ **NOT VERIFIED — unreproduced across an exhaustive retest; downgrading confidence that this is a real defect.** A prior independent audit (2026-07-07, finding D-1) reported that following the documented Quick Start exactly produced real `tsc` compile errors from `streetjs`'s `websocket/server.d.ts` referencing `ws`'s (unshipped) types. Retested twice across two sessions (2026-07-08): first with a hand-written minimal project, then — more decisively — with the **real, unmodified `npx @streetjs/cli@1.1.1 create` scaffold**, varying every plausible variable: package version (real `^1.0.6`→`1.0.25` resolution, and explicit `1.1.1`), TypeScript version (5.9.3 and 6.0.3), `skipLibCheck` (true/false), `@types/ws` presence (removed it from the real scaffold's `node_modules` directly), and forcing TS to resolve the exact flagged type via two dedicated probes (`RawWsHandler` from `streetjs/websocket`, and importing `WebSocket` from `ws` directly). **Every single combination compiled clean, 0 errors.** This does not prove the original finding was wrong — but two independent, careful, multi-variable attempts finding zero reproductions is strong evidence it is not a real, current defect. **Not claimed fixed** — no code change was made. | [MAINTAINER] | `docs/audits/2026-07-07-final-independent-audit.md` finding D-1; two retest sessions 2026-07-08 | A third-party bug report with a real failing repro, or explicit closure as a false positive if none surfaces |
| 34 | ✅ **Fixed + verified (2026-07-08) — the CLI scaffold's `streetjs` dependency was pinned `^1.0.6`, so `npx @streetjs/cli create` generated new projects resolving to `streetjs@1.0.25`, not the current release.** Discovered while reproducing #33: a fresh `create` + `npm install` installed `1.0.25` (`^1.0.6` cannot cross the `1.1.x` minor boundary under semver), meaning every new project scaffolded was missing the v1.1.1 path-traversal fix and everything else shipped since `1.0.6`. **Fix:** bumped the hardcoded template dependency in `packages/cli/src/commands/create.ts` to `^1.1.1` (matching `packages/cli`'s own already-correct `streetjs` dependency). **Verified:** rebuilt the CLI, ran the affected test suites (65/65 pass), then did a real, end-to-end `create` → `npm install` → confirmed `node_modules/streetjs/package.json` resolves to `1.1.1` → `tsc --noEmit` clean. | [MAINTAINER] | discovered during #33 retest, 2026-07-08; fixed same day | `packages/cli/src/commands/create.ts` scaffold dependency; verified via fresh `create`+`install` resolving to `1.1.1` |

| 35 | ✅ **Fixed + verified (2026-07-09) — `Platform Leadership exit-criteria aggregation` no longer blocks PRs.** Added an `--advisory` flag to `street verify --aggregate` (`packages/cli/src/commands/verify.ts`): decision still computed solely by the aggregator and persisted verbatim, but exits 0 regardless of GRANTED/WITHHELD (strict exit 1 preserved without the flag). Wired `.github/workflows/platform-leadership.yml` to pass `--advisory` only on `pull_request` events, staying STRICT on `workflow_dispatch`/`schedule` where the artifacts genuinely exist — so the zero-trust guarantee (Req 12.3/12.4) is intact where authoritative. Verified: builds clean; empty-artifacts sandbox → strict exit 1 / advisory exit 0 with same WITHHELD decision. Also confirmed this check was never in `main`'s branch-protection required list (so it was advisory noise, not a hard gate). GitHub issue #118 closed as completed. _Original issue:_ the gate ran `street verify --aggregate` which reported `WITHHELD` on PRs because per-capability artifacts (`dast.scan`, `cloud.deploy`, `release.scorecard`, `devx.*`, etc.) are only produced by heavyweight/scheduled jobs; forced an `--admin` merge of Dependabot PR #117 on 2026-07-09. Related to #31 (`release.scorecard`/`release-inputs.json` gap). The gate runs `street verify --aggregate`, which reports `WITHHELD` because it requires verification artifacts (`dast.scan`, `cloud.deploy`, `registry.publish-install`, `plugins.ecosystem`, `enterprise.api`, `devx.*`, `kafka.chaos`, `observability.validate`, `release.scorecard`) produced only by heavyweight/scheduled jobs that do not run (or upload consumable artifacts) in a PR context, so every artifact is `NOT_IMPLEMENTED (no artifact)`. Only triggers on PRs that touch `.github/workflows/**`. Confirmed live on Dependabot PR #117 (a routine actions bump, otherwise fully green), which had to be `--admin`-merged 2026-07-09. **Do NOT fix by fabricating artifacts** (defeats the zero-trust design, same principle as #31). Same class as #31 (`release.scorecard`/`release-inputs.json` gap) — `release.scorecard` is one of the withheld criteria here. **Fix options:** (a) make this check non-required on PRs and run it only on `main`/scheduled/release events where its artifacts exist (mirroring how `kafka-chaos` / `Release Engineering Enforcement` are already handled); or (b) have `street verify --aggregate` treat "artifact absent because its producing job doesn't run in this event" as `SKIPPED`/`N/A` rather than `WITHHELD → exit 1`. Tracked as GitHub issue #118. | [MAINTAINER] | GitHub issue #118 (this session, 2026-07-09); failing run on PR #117 | A workflow-touching PR passes all required checks without `--admin`, and the aggregation still runs meaningfully on events where its artifacts are genuinely produced |

| 36 | ✅ **Resolved (2026-07-10) — alerts closed; history purge deliberately SKIPPED after risk review.** GitHub secret-scanning opened **16 alerts (#1–#16, `google_api_key`)**, all detected in the **same committed gitleaks tool binary** `.release-verify-tmp3/gitleaks` (21 MB) at consecutive lines 19541–19556 — plus `.gitleaks.tar.gz` (8 MB) — added in commit `0a7147b4` during an ad-hoc release-verify run. All 16 are **gitleaks' own detection-rule test fixtures inside the third-party binary — not StreetJS credentials** (nothing to rotate/revoke); e.g. #4 `AIzaSyabcdef…1234567` is a literal alphabet and #11 has a malformed `AIzay` prefix. Same stray class also committed `.release-verify-tmp/` (`fe09903b`) and `.release-verify-tmp2/` (`3f9408cb`/`5ec72378`/`0f1cf4a3`). **Done (non-destructive, on `main`):** (a) `.gitignore` now ignores `.release-verify-tmp*/` + `release-verify-tmp*/` (verified — prevents recurrence); (b) `.gitleaks.toml` PATH-allowlists `(^|/)\.release-verify-tmp` (covers all 16 fixture keys at once, not per-value) so the repo's own gitleaks gate does not re-flag them; (c) operator runbook written: `security/RELEASE-VERIFY-TMP-PURGE-RUNBOOK.md`. **All 16 alerts CLOSED (2026-07-10, operator-verified)** as `resolved`/`used_in_tests` via `gh api -X PATCH .../secret-scanning/alerts/{n}` — verified: `gh api .../secret-scanning/alerts` shows all of #1–#16 `resolved`/`used_in_tests`. **History purge DELIBERATELY NOT RUN:** with the alerts closed, prevention in place, and the strings being non-exploitable third-party fixtures, the `git filter-repo` + force-push was judged not worth its cost — it would unsign all 4920 commits (degrading P0 control #5), change every SHA (breaking clones/PR refs), require temporarily disabling force-push protection, and still need a GitHub Support request for PR-ref/cache purge. A verified-clean purge mirror was prepared and validated (2265 files / 4920 commits intact, blob absent) but discarded unrun. Recovery SHA of live `main` at decision time: `ae2afa38`. Runbook retained for future operator use if a real leak ever occurs. `.gitleaks.toml` allowlist kept as documentation. | [MAINTAINER]+[OPERATOR] done | GitHub alerts #1–#16 (all resolved); commit `0a7147b4`; `security/RELEASE-VERIFY-TMP-PURGE-RUNBOOK.md` | ✅ Alerts #1–#16 closed; recurrence prevented; purge intentionally skipped per risk review |

| 37 | ✅ **Fixed + verified (2026-07-10) — CodeQL alert #170 (`js/polynomial-redos`) in `packages/gateway/src/auth.ts:62`.** The bearer-token extractor used `/^Bearer[ \t]+(.+)$/i`; because `.` also matches space/tab, `[ \t]+` and `(.+)` overlap, so on adversarial input (`"bearer"` + many `\t` with no non-whitespace token) the backtracking engine partitions the whitespace run O(n) ways before failing → quadratic. **Fix:** replaced the regex with a linear, backtracking-free string scan (case-insensitive `Bearer` scheme check via `slice(0,6).toLowerCase()`, then advance past `[ \t]` separators, requiring ≥1, then `slice().trim()` the remainder) — exact same contract (scheme, ≥1 separator, non-empty trimmed token), no regex. **Verified:** `tsc` clean, no diagnostics; full gateway suite **252/252 pass, 0 skip**; added 4 regression tests incl. an adversarial `"bearer"+"\t"*200000` input that now resolves to `null` in <1000ms (was quadratic). Committed to `main` (`c3ce8043` src, `72b411ba` tests). | [RUNTIME] done | CodeQL alert #170; commit `71423ae`; `packages/gateway/src/auth.ts` | ✅ Alert #170 resolved; linear parser + ReDoS regression test |

| 38 | ✅ **Fixed + verified (2026-07-10) — Scorecard Pinned-Dependencies #173 (`vendor-integration.yml`).** The `storage` live-round-trip job installed the optional cloud SDKs with a mutable `npm install @supabase/supabase-js @google-cloud/storage @azure/storage-blob --package-lock=false --prefix packages/storage`. Per Scorecard's own source (`isNpmUnpinnedDownload`), **only `npm ci` or a git-URL pinned to a 40-hex commit hash counts as pinned** — an `npm install pkg@version` is still flagged, so version-pinning alone would not clear it. **Fix:** added a dedicated, non-workspace, pinned project `.github/ci/live-sdks/` (`package.json` exact-pins Supabase `2.110.2` / GCS `7.21.0` / Azure `12.33.0`; committed `package-lock.json`, 112 pkgs, all with integrity hashes) and switched the step to `npm ci --prefix .github/ci/live-sdks` then `cp -R` the resolved trees onto `packages/storage/node_modules` (needed because the package is ESM — `NODE_PATH` isn't consulted — and a workspace-member install hits the documented npm 10.8.2 Arborist bug). **Verified:** `npm ci` installs cleanly against the lockfile; all three SDKs resolve via ESM `import()` from `packages/storage`; workflow YAML parses; no diagnostics. **Transitive `uuid` advisory RESOLVED via override (2026-07-10, after Dependabot alert #15):** the initial lockfile pulled `uuid@9.0.1` transitively under `@google-cloud/storage@7.21.0` (`gaxios`/`teeny-request`, which pin `^9`) — GHSA-w5hq-g745-h8pq / CVE-2026-41907 (out-of-bounds write in `v3/v5/v6` when a caller buffer is supplied; `v4`, which the SDK actually uses, is unaffected). Because `.github/ci/live-sdks/` is a fully isolated project, added an npm `overrides: { "uuid": "11.1.1" }` forcing the whole tree onto the patched **uuid 11.1.1**. Chosen over `14.x` (ESM-only major — would break `gaxios`/`teeny-request`'s CJS `require('uuid')`) and over leaving `9.0.1` (Dependabot alert). **Verified:** `uuid@11.1.1` keeps a CJS `require` conditional export, so `require('uuid').v4()` still works; `npm ci` clean; **`npm audit` → 0 vulnerabilities**; all three SDKs load (`Storage`/`createClient`/`BlobServiceClient`). Dependabot alert #15 will auto-close on next scan. These SDKs remain test-only, credential-gated, and never in any published artifact. | [MAINTAINER] done | Scorecard alert #173; Dependabot alert #15; `.github/workflows/vendor-integration.yml`; `.github/ci/live-sdks/` | ✅ Install is `npm ci` (Scorecard-pinned), lockfile integrity-hashed, `npm audit` clean, SDKs resolve |

| 39 | ◑ **Investigated — no code change; config already optimal (2026-07-10).** Scorecard SAST #116 ("SAST tool detected but not run on all commits: 1 of 3 commits checked"). `.github/workflows/codeql.yml` already triggers on push→`main`, PR→`main`, and a weekly schedule, and its `concurrency` block is **already deliberately tuned for exactly this metric**: main pushes are grouped per-`github.sha` with `cancel-in-progress: false` so every default-branch commit completes its own SAST run (PR runs are per-ref and cancellable). The rolling "N of M commits checked" ratio self-heals as CodeQL analyses finish on `main`; it dips transiently when commits land faster than analyses complete — which this environment's auto-commit-on-every-change cadence aggravates. There is no config change that meaningfully raises the ratio without slowing the commit cadence; churning `codeql.yml` would only undo prior deliberate tuning. **Not a config defect.** | [MAINTAINER] | Scorecard alert #116; `.github/workflows/codeql.yml` | Ratio recovers as consecutive main-push CodeQL runs complete; no action beyond existing config |

| 40 | ◑ **cosign v4 signing migration [MAINTAINER] — pin applied, migration pending.** The `v1.1.2` tag-triggered `ci-cd.yml` run failed at the tag-only *"Pack and sign release tarballs"* step (`cosign sign-blob … create bundle file: open : no such file or directory`) after the actions bump moved `sigstore/cosign-installer` v3.7.0→v4.1.2 (cosign v4 defaults to the new bundle format, breaking `--output-signature/--output-certificate`). **Did NOT affect npm publish/provenance** (signing runs after publish, by design; v1.1.2 verified on npm with SLSA provenance). **Applied fix:** pinned `cosign-installer` back to the known-good v3.7.0 (signed v1.1.1) in `.github/workflows/ci-cd.yml`. **Remaining:** migrate the step to cosign v4 (`cosign sign-blob --bundle <f>.bundle …`) and validate on a throwaway test tag before dropping the pin — NOT done blind (unverifiable without a tag run). v1.1.2's GitHub Release has no signed `.tgz` assets as a result. | [MAINTAINER] | `ci-cd.yml` sign step; failed run 29078716545 | v4 sign step succeeds on a test tag; pin removed |

| 41 | ✅ **`scripts/release.sh` rewritten to the CI-driven model (2026-07-10).** The old script was outdated for the current monorepo: it bumped only core+cli (name-mismatched: `packages/core` is `streetjs`, not `@streetjs/core`), never regenerated the `@streetjs/core` compat shim (`packages/core-compat`), and ran a **local `npm publish`** (no provenance, wrong package set). Rewrote it as a **prepare-only** script: bumps the lockstep trio (streetjs + @streetjs/cli + regenerated core-compat) + scaffold pin + root lockfile, builds/tests/pack-validates, verifies lockstep, commits, tags, and **pushes** — then CI (`ci-cd.yml`) publishes with provenance. No local publish. Verified: `bash -n` clean; `--dry-run` computes 1.1.2→1.1.3 and changes nothing. | [MAINTAINER] | `scripts/release.sh` | ✅ prepare-only, CI publishes; dry-run verified |

| 42 | ◑ **CHANGELOG duplicate-version defects fixed; one ordering note for maintainer review.** Resolved the two Phase-3-blocking duplicates: the stray mid-history `## [Unreleased]` (Kafka/RabbitMq/browser/cloud) → `## [1.0.6] - 2026-06-09` (matches the real `v1.0.6` tag date + Kafka landing 2026-06-06; no prior 1.0.6 entry), and the duplicate `## [1.0.3]` (aligned to the authoritative `v1.0.3` tag date 2026-05-28 and merged). Also added the `## [1.1.2] - 2026-07-10` release section. Verified: exactly 1 `[Unreleased]`, no duplicate version headings. **Note for maintainer:** the relabeled `[1.0.6]` block sits slightly out of chronological order (appears between 1.0.10 and 1.0.9 rather than below 1.0.7) — valid + evidence-based attribution, but position is imperfect; confirm/move if desired. | [MAINTAINER] | `CHANGELOG.md` | ✅ no duplicates; ordering of relabeled 1.0.6 to confirm |

## P3 — Long-term
| # | Action | Owner | Status |
|---|---|---|---|
| 24 | SOC 2 readiness | [OPERATOR] (external audit) | Org program — requires an audit firm + control evidence period; cannot be repo-completed. Tracked in `SECURITY-ROADMAP.md`. |
| 25 | ISO 27001 alignment | [OPERATOR] (external audit) | Org program — ISMS + certification body; cannot be repo-completed. Tracked in `SECURITY-ROADMAP.md`. |
| 26 | OpenSSF Best Practices badge | [MAINTAINER]+[OPERATOR] | ◑ **Evidence pack done** — "passing"-tier self-assessment added to `OPENSSF-REVIEW.md`. Remaining: register + submit on bestpractices.dev (operator) + the listed gating gaps (branch protection, push protection, PGP key). |
| 27 | Security Champions program + dual-control releases | [OPERATOR]/community | Org/process — needs people + role assignment; framework in `CONTRIBUTOR-GOVERNANCE.md`. Cannot be repo-completed. |
| 28 | Grow MAINTAINERS / security team (bus-factor) | [OPERATOR]/community | Org/hiring — cannot be repo-completed; governance + neutral-maintainership path documented in `governance/DECISION-PROCESS.md`. |

> **P3 status:** #26 has its maintainer evidence pack done (badge submission is
> operator). #18 (OSS-Fuzz) and #12 (keyless signing) are partially specced; their
> remaining work is external/operator. #24/#25/#27/#28 are organizational programs
> that **cannot be completed by editing the repository** — they require external
> audits, badge registration, or staffing. They are documented with owners here and
> in the roadmap so nothing is lost, but they are explicitly out of repo scope.

## Sequencing
1. **P0 first** (branch/push protection, history purge, key relocation) — these unlock the biggest score gains (Security 70→86, SLSA L2→L3 path) and close the only HIGH residuals.
2. **P1 governance/org** in one branch (CODEOWNERS teams, lockfiles, app-* move, SEO removal); **P1 runtime** (timeouts, webhook verifiers) as a separate plugin PR.
3. **P2/P3** iteratively.

## Notes
- **[RUNTIME]** items #8 (timeouts, 9 plugins), #9 (all 4 webhook verifiers), and
  #15 (TLS for all 5 redis/mongodb/kafka/rabbitmq/nats) are **done** —
  implemented additively in `packages/core` + the separate plugin packages, with
  tests, after the no-touch-core constraint was explicitly lifted for these
  scoped, tested changes. The only remaining [RUNTIME] item is #21 (per-plugin
  example apps + raised coverage gates), which is incremental and in progress.
- Verification of P0 platform items can't be done from the repo; export them as
  settings-as-code (e.g. an `allstar`/repo-settings file) for auditability. See
  `security/OPERATOR-EXECUTION-CHECKLIST.md` for the sequenced `gh` commands.
